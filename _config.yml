# ---- Site settings -----------------------------------------------------------
title: "Access"
description: "Talent is everywhere. What's rare is a way in."
author: "Nadir"
lang: en
# url/baseurl are filled in by the setup; GitHub Pages also sets url automatically.
url: "https://nadosaurusrex.github.io"
baseurl: ""
# Optional newsletter import source for tools/sync.py (empty = disabled):
substack_url: ""

# ---- Build settings (GitHub Pages' built-in Jekyll; no Actions workflow needed) ----
markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false
permalink: /p/:title/
timezone: Europe/London
future: true
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap
feed:
  posts_limit: 50
defaults:
  - scope: {path: "", type: posts}
    values: {layout: post}
  - scope: {path: ""}
    values: {layout: default, image: /assets/img/og/card.png}
exclude:
  - tools/
  - posts/
  - drafts/
  - README.md
  - SETUP_LOG.md
  - Publish.command
  - Gemfile
  - Gemfile.lock
  - vendor/

# ---- Search engines (added 2026-08-19) ----
webmaster_verifications:
  google: vXF10ReIlY4i7WWEmc1wnZA4Mf8O-CELhpE5cZjtRxo
